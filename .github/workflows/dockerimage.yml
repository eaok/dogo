name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: go-docker
      run: docker build . --file Dockerfile --tag go-docker:$(date +%s)
